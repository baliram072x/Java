1. Encapsulation ==> is defined as the wrapping up of data & methods under a single unit 
it also impiments data hiding 

2. Constructors ==> is a special methods which is evoked automatically at time of object creation
- have same name as class class or sturcture 
- dont have  a return type
- are called once at object creation 
- memory allocation happens when costructor is called 